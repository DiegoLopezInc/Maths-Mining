# OpenAI API Key for agentic validation
OPENAI_API_KEY=your_api_key_here

# Model to use for validation (default: gpt-4)
OPENAI_MODEL=gpt-4

# Temperature for generation (0.0 - 1.0, lower is more deterministic)
TEMPERATURE=0.1
